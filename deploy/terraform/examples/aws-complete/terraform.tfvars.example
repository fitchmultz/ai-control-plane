#------------------------------------------------------------------------------
# AWS Complete Example - Terraform Variables Example
#------------------------------------------------------------------------------
# Copy this file to terraform.tfvars and customize for your environment.
# DO NOT commit terraform.tfvars with real values to Git.
#------------------------------------------------------------------------------

#------------------------------------------------------------------------------
# General Configuration
#------------------------------------------------------------------------------

# AWS region for deployment
aws_region = "us-east-1"

# Environment: production is the supported primary deployment baseline
environment = "production"

# Prefix for all resource names
name_prefix = "ai-control-plane"

# Additional tags for all resources
tags = {
  Team        = "platform"
  CostCenter  = "engineering"
  Owner       = "ai-team@example.com"
}

#------------------------------------------------------------------------------
# VPC Configuration
#------------------------------------------------------------------------------

# VPC CIDR block
vpc_cidr = "10.0.0.0/16"

# Availability zones (leave empty to use first 3 AZs in the region)
availability_zones = []

# Private subnet CIDRs (one per AZ)
private_subnet_cidrs = ["10.0.1.0/24", "10.0.2.0/24", "10.0.3.0/24"]

# Public subnet CIDRs (one per AZ)
public_subnet_cidrs = ["10.0.101.0/24", "10.0.102.0/24", "10.0.103.0/24"]

# Use redundant NAT gateways for production resilience
single_nat_gateway = false

#------------------------------------------------------------------------------
# EKS Configuration
#------------------------------------------------------------------------------

# Kubernetes version
cluster_version = "1.29"

# Keep the cluster API private unless you have an explicit admin allowlist
cluster_endpoint_public_access = false

# Enable private access to cluster endpoint
cluster_endpoint_private_access = true

# CIDR blocks allowed for public cluster access when explicitly enabled
cluster_public_access_cidrs = []

# EKS node groups configuration
node_groups = {
  general = {
    desired_size   = 2
    min_size       = 1
    max_size       = 5
    instance_types = ["t3.medium"]
    capacity_type  = "ON_DEMAND"
    disk_size      = 50
    labels = {
      role = "general"
    }
  }
}

# Enable cluster autoscaler IAM permissions
enable_cluster_autoscaler = true

#------------------------------------------------------------------------------
# RDS Configuration
#------------------------------------------------------------------------------

# RDS instance class (environment-specific defaults exist)
#   dev:        db.t3.micro
#   staging:    db.t3.small
#   production: db.t3.medium
rds_instance_class = "db.t3.medium"

# PostgreSQL engine version
rds_engine_version = "16.3"

# Initial storage size in GB
rds_allocated_storage = 20

# Maximum storage size in GB for autoscaling
rds_max_allocated_storage = 100

# Enable Multi-AZ for high availability
rds_multi_az = true

# Backup retention period in days
rds_backup_retention_period = 7

# Enable deletion protection
rds_deletion_protection = true

# Preserve a final snapshot when destroying
rds_skip_final_snapshot = false

# Enable Performance Insights
rds_performance_insights_enabled = true

# Database name
rds_database_name = "litellm"

# Database username
rds_username = "litellm"

#------------------------------------------------------------------------------
# Ingress Configuration
#------------------------------------------------------------------------------

# Expose ingress publicly only when required
public_ingress_enabled = false

# ACM certificate ARN (required when ingress is enabled)
alb_certificate_arn = "arn:aws:acm:us-east-1:123456789012:certificate/replace-me"

#------------------------------------------------------------------------------
# Helm Chart Configuration
#------------------------------------------------------------------------------

# Kubernetes namespace
namespace = "acp"

# Helm release name
helm_release_name = "acp"

# Number of LiteLLM replicas
litellm_replica_count = 2

# LiteLLM resource limits and requests
litellm_resources = {
  limits = {
    cpu    = "1000m"
    memory = "1Gi"
  }
  requests = {
    cpu    = "250m"
    memory = "512Mi"
  }
}

# LiteLLM master key (required)
litellm_master_key = "replace-with-32-character-minimum-master-key"

# LiteLLM salt key (required)
# WARNING: Never change this after initial deployment - it will corrupt encrypted data
litellm_salt_key = "replace-with-32-character-minimum-salt-key"

# Enable autoscaling
enable_autoscaling = true

# Enable Kubernetes ingress only after DNS and certificates are ready
enable_ingress = false

# Ingress hostname (required)
ingress_host = "ai-control-plane.example.com"

# Ingress class name (e.g., alb, nginx, traefik)
ingress_class_name = "alb"
