# Offline / Airgap Example

Use this example for deterministic local validation, demos, and CI-like runtime checks.

## Commands

```bash
make validate-config
make up-offline
make health
make ci-runtime-checks
```

## References

- `demo/docker-compose.offline.yml`
- `demo/config/litellm-offline.yaml`
